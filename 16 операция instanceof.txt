позволяет узнать тип объекта, его общая форма: объектная-ссылка instanceof тип (результат будет true или false)
также операция instanceof дает true, когда объект является экземпляром подкласса указанного типа
    Если ссылка суперкласса ссылается на объект подкласса, то будет true и ссылка alpha может быть приведена к Beta,

    сопоставления с образцом:
механизм, который определяет, соответствует ли значение общей форме. Используется для проверки типа значения (которое
должно быть ссылочным типом) на соответствие указанному типу Если шаблон совпадает, тогда шаблонная переменная получит
ссылку на объект, соответствующий шаблону. объектная-ссылка instanceof тип шаблонная-переменная

Number myOb = Integer.valueOf(27); Использовать версию instanceof, поддерживающую сопоставление с образцом.
if(myOb instanceof Integer iObj) { // Здесь переменная iObj известна и находится в области видимости.
    System.out.println("iObj ссылается на целое число: " + iObj);
}
// Здесь iObj не существует.
